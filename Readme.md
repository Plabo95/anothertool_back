# Create Virtual Env

    python3 -m venv /path/to/new/virtual/environment
    or
    python -m venv venv

# Activate Virtual Env

    cd venv/Scripts
    activate
    pip install -r requirements.txt

# Freeze the packages

    python -m pip freeze
